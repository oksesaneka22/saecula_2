# todo in future
тепер зроби так щоб структури будувались з блоків, типу ти робиш блупрінт, і там блупрінт треба будувати поблоково, але це тільки саме для структур, для меблів(типу багаття), такого не потрібно, там зразу структура як блок
