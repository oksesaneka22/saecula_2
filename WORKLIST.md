# Saecula: Базовий план розробки (WORKLIST)

> **Принцип:** Не генерувати все підряд. Кожна фіча — це мікро-ітерація з атомарним промптом, тестом і чекпоінтом у `CHANGES.md`.  
> **Цільова якість:** 100% стабільний фундамент, готова архітектура для масштабування від пісочниці виживання до колонії масштабу Minecolonies.
> **Роздільна здатність:** Нативна підтримка 1920x1080 (Full HD) та 2560x1440 (2K QHD).

---

## Статус проекту

- [x] **Етап 1: Стабільний фундамент проекту (Foundations)**
  - [x] Ітерація 1.1: Ініціалізація структури папок та конфігурації Godot 4
  - [x] Ітерація 1.2: EventBus та базовий GameManager
- [x] **Етап 2: Тайлова сітка та Навігація (Grid & Navigation)**
  - [x] Ітерація 2.1: GridManager та AStarGrid2D
  - [x] Ітерація 2.2: Базова тестова сцена світу з TileMapLayer
- [x] **Етап 3: Персонаж гравця та Камера (Player & Camera)**
  - [x] Ітерація 3.1: Контролер гравця (Player) та плавний рух
  - [x] Ітерація 3.2: Камера з зумом та слідуванням (GameCamera2D)
- [ ] **Етап 4: Інвентар та Ресурси на карті (Items, Harvesting & Inventory)**
  - [x] Ітерація 4.1: Схеми предметів (Data-Driven ItemData & ItemDatabase)
  - [x] Ітерація 4.2: Компонент інвентаря (`InventoryComponent.gd`)
  - [X] Ітерація 4.3: Природні об'єкти на карті (Дерево, Камінь) та дроп
  - [ ] Ітерація 4.4: UI швидкого доступу (Hotbar) та Інвентар гравця
- [ ] **Етап 5: Крафт та Перші інструменти (Crafting System)**
  - [ ] Ітерація 5.1: Схема рецептів та менеджер крафту
  - [ ] Ітерація 5.2: UI меню крафту
- [ ] **Етап 6: Розміщення споруд та Креслення (Placement & Blueprint Preview)**
- [ ] **Етап 7: Склади та Логістика предметів (Stockpiles & Containers)**
- [ ] **Етап 8: Агент-Колоніст та FSM (Colonist Agent & Navigation)**
- [ ] **Етап 9: Система завдань Minecolonies (Job Manager & Work Orders)**
- [ ] **Етап 10: Будівельник і покрокове будівництво (Builder Colonist)**
- [ ] **Етап 11: Професії та Будинки робітників (Worker Huts & Roles)**
- [ ] **Етап 12: Система Епох та Дослідження (Era Progression: Stone -> Bronze)**
- [ ] **Етап 13: Збереження та Завантаження (Save/Load Serialization)**
- [ ] **Етап 14: UI, Полірування та Зворотний зв'язок (HUD & Polish)**

---

# Етап 1: Стабільний фундамент проекту

### Ітерація 1.1: Ініціалізація структури папок та конфігурації Godot 4
* **Мета:** Створити правильну модульну структуру директорій та базовий `project.godot` з обов'язковою підтримкою 1920x1080 та 2560x1440.
* **Чекліст:**
  - [x] Створити структуру папок: `assets/`, `data/schemas/`, `src/core/`, `src/world/`, `src/entities/`, `src/systems/`, `src/ui/`.
  - [x] Створити `project.godot` з налаштуваннями: роздільна здатність вікна з обов'язковою та точною підтримкою 1920x1080 (Full HD) та 2560x1440 (2K QHD) (базовий viewport 1920x1080, stretch mode: `canvas_items`, aspect: `keep`), рендерер `gl_compatibility` (максимальна сумісність і стабільність).
  - [x] Налаштувати базові дії введення (Input Map): `move_up` (W/Up), `move_down` (S/Down), `move_left` (A/Left), `move_right` (D/Right), `interact` (E), `cancel` (Escape), `hotbar_1`..`hotbar_9`.
* **Промпт для генерації:**
  > "Створи базову конфігурацію проекту Godot 4: згенеруй `project.godot` з обов'язковою та точною підтримкою роздільних здатностей 1920x1080 та 2560x1440 (viewport 1920x1080, stretch mode canvas_items, aspect keep), pixel snap для 2D, налаштованою мапою клавіш (WASD, Interact 'E', Cancel 'Escape') та структуру порожніх модульних директорій згідно з [DESCRIPTION.md](file:///C:/Users/Sasha/OneDrive%20-%20UCU/Робочий%20стіл/work_dir/personal/saecula_2/DESCRIPTION.md)."
* **Критерій готовності:** Проект відкривається або запускається у Godot 4 без помилок конфігурації, коректно масштабується і чітко відображається як у 1920x1080, так і у 2560x1440.

---

### Ітерація 1.2: EventBus та базовий GameManager
* **Мета:** Створити глобальну шину подій для усунення жорстких зв'язків між підсистемами.
* **Чекліст:**
  - [x] Створити `src/core/EventBus.gd` з сигналами для зміни стану гри, зміни часу/дня, спавну предметів, оновлення інвентаря, створення завдань.
  - [x] Створити `src/core/GameManager.gd` з базовим скінченним автоматом станів гри (`GameState`: `PLAYING`, `PAUSED`, `COLONY_OVERVIEW`, `BUILDING_MODE`).
  - [x] Налаштувати реєстрацію обох скриптів як Autoloads у `project.godot`.
  - [x] Додати обробку `cancel` (клавіша Escape) для відкриття паузи або виходу з режиму.
* **Промпт для генерації:**
  > "Створи `src/core/EventBus.gd` з глобальними сигналами гри (зміна стану, інвентар, завдання, епохи) та `src/core/GameManager.gd` як центральний контролер станів гри з Enum `GameState` та контролем часу. Додай їх у `project.godot` як Autoloads."
* **Критерій готовності:** Проект запускається, обидва Autoloads доступні глобально з будь-якого вузла дерева сцен.

---

# Етап 2: Тайлова сітка та Навігація (Grid & Navigation)

### Ітерація 2.1: GridManager та AStarGrid2D
* **Мета:** Створити систему координат сітки та базову навігаційну сітку для пошуку шляхів.
* **Чекліст:**
  - [x] Створити `src/world/GridManager.gd` як Autoload.
  - [x] Розмір клітинки за замовчуванням: 32x32 пікселі (`TILE_SIZE = 32`).
  - [x] Інтегрувати `AStarGrid2D`: налаштувати розмір області, діагональний рух, штрафи та вимкнути перетин непрохідних кутів (`jumping = false`).
  - [x] Методи: `world_to_map(pos: Vector2) -> Vector2i`, `map_to_world(cell: Vector2i) -> Vector2`, `is_cell_solid(cell: Vector2i) -> bool`, `set_cell_solid(cell: Vector2i, solid: bool)`.
  - [x] Метод `get_world_path(start: Vector2, end: Vector2) -> PackedVector2Array`.
* **Промпт для генерації:**
  > "Реалізуй Autoload `src/world/GridManager.gd` з розміром тайла 32x32 та інтеграцією `AStarGrid2D` для знаходження шляхів. Додай методи перетворення координат `world_to_map` та `map_to_world`, методи перевірки прохідності клітинок та генерацію шляху `get_world_path`."
* **Критерій готовності:** Метод `get_world_path()` повертає валідний масив точок між двома координатами у світі.

---

### Ітерація 2.2: Базова тестова сцена світу з TileMapLayer
* **Мета:** Відобразити тайлову поверхню у вікні гри та відлагоджувальну сітку для розробника.
* **Чекліст:**
  - [x] Створити сцену `src/world/World.tscn` з кореневим вузлом `Node2D` (`World.gd`).
  - [x] Додати вузол `TileMapLayer` (`GroundLayer.gd`) для рендерингу землі/трави.
  - [x] Створити процедурний тайлсет 32x32.
  - [x] Додати візуальний режим відладки сітки (Debug Grid Draw, клавіша `F3`).
  - [x] Зробити `World.tscn` стартовою або дочірньою сценою в `Main.tscn`.
* **Промпт для генерації:**
  > "Створи сцену `World.tscn` з `TileMapLayer` для землі з розміром тайла 32x32. Додай скрипт `GroundLayer.gd`, що генерує базову трав'яну поверхню, та можливість вмикати відлагоджувальну сітку по клавіші `F3`."
* **Критерій готовності:** При запуску гри відображається поле з тайлів, натискання `F3` показує/ховає лінії сітки 32x32, гра масштабується під 1080p/1440p без спотворень.

---

# Етап 3: Персонаж гравця та Камера (Player & Camera)

### Ітерація 3.1: Контролер гравця (Player) та плавний рух
* **Мета:** Реалізувати персонажа гравця з прямим керуванням WASD, плавним прискоренням та колізією.
* **Чекліст:**
  - [x] Створити сцену `src/entities/player/Player.tscn` (`CharacterBody2D`).
  - [x] Додати `CollisionShape2D` (радіус колізії 8 пікселів).
  - [x] Написати `Player.gd`: плавний 8-напрямний рух через `Input.get_vector()`, нормалізація діагоналі, швидкість 140 пікселів/с, прискорення та тертя.
  - [x] Додати збереження напрямку погляду `facing_direction` (Vector2).
  - [x] Додати плейсхолдер спрайта або процедурне малювання персонажа через `_draw()` (тіло, голова, напрямок погляду).
  - [x] Додати інстанс `Player.tscn` у `World.tscn`.
* **Промпт для генерації:**
  > "Створи персонажа гравця `src/entities/player/Player.tscn` (`CharacterBody2D`) з колізією під тайли 32x32, плавним рухом WASD у 8 напрямках, нормалізацією діагоналі та процедурним візуалом або спрайтом, що показує напрямок руху."
* **Критерій готовності:** Гравець вільно рухається по карті, не проходить крізь межі та колізії, плавно зупиняється, чітко видно напрямок погляду.

---

### Ітерація 3.2: Камера з зумом та слідуванням (GameCamera2D)
* **Мета:** Реалізувати плавне слідування камери за гравцем з можливістю наближення/віддалення колесом миші, оптимізовано під монітори 1080p та 1440p.
* **Чекліст:**
  - [x] Створити вузол `src/core/GameCamera2D.gd` (`Camera2D`).
  - [x] Налаштувати плавний зум коліщатком миші (обмеження від 0.5x до 2.5x).
  - [x] Додати інтерполяцію зміни зуму через `Tween` для плавності.
  - [x] Прив'язати камеру до гравця в `Player.tscn` або центровано в `World.tscn`.
* **Промпт для генерації:**
  > "Створи вузол `GameCamera2D.gd`, який слідує за гравцем, підтримує плавний зум через коліщатко миші (діапазон 0.5x – 2.5x через Tween) та адаптований під 1920x1080 і 2560x1440."
* **Критерій готовності:** Камера слідує за гравцем, коліщатко наближає/віддаляє картинку плавно без ривків, огляд чудово виглядає на обох цільових роздільних здатностях.

---

# Етап 4: Інвентар та Ресурси на карті (Items, Harvesting & Inventory)

### Ітерація 4.1: Схеми предметів (Data-Driven ItemData & ItemDatabase)
* **Мета:** Створити Data-Driven архітектуру предметів на основі кастомних `Resource` (`.tres`) згідно з `skill-resource-schema.md`.
* **Чекліст:**
  - [x] Створити схему `src/data/schemas/ItemData.gd`: `id`, `display_name`, `category`, `tool_type`, `tier`, `max_stack`, `icon`.
  - [x] Створити допоміжну схему `src/data/schemas/ItemCost.gd`: `item`, `amount`.
  - [x] Створити базові предмети Кам'яного віку у `data/items/`: `wood.tres`, `stone.tres`, `flint.tres`, `berries.tres`.
  - [x] Створити Autoload `src/core/ItemDatabase.gd`, що автоматично завантажує та кешує всі предмети з папки `data/items/` і надає доступ через `ItemDatabase.get_item(&"wood")`.
* **Промпт для генерації:**
  > "Реалізуй систему ресурсів згідно з `skill-resource-schema.md`: створи `ItemData.gd`, `ItemCost.gd`, базові предмети деревини, каменю, кременю та ягід у `data/items/`, а також `ItemDatabase.gd` для автоматичного сканування папки з ресурсами."
* **Критерій готовності:** У консолі чи коді виклик `ItemDatabase.get_item(&"wood")` повертає валідний об'єкт `ItemData` з коректними параметрами.

---

### Ітерація 4.2: Компонент інвентаря (`InventoryComponent.gd`)
* **Мета:** Створити універсальний компонент інвентаря, придатний для гравця, жителів колонії, скринь та будівельних складів.
* **Чекліст:**
  - [x] Створити `src/systems/inventory/InventorySlot.gd` (`RefCounted`).
  - [x] Створити `src/systems/inventory/InventoryComponent.gd` (`Node`).
  - [x] Логіка стакування за `item.max_stack`: спершу заповнюються існуючі слоти з тим самим предметом, потім вільні слоти.
  - [x] Методи: `add_item(item, amount) -> int` (повертає залишок при переповненні), `remove_item(item_id, amount) -> bool`, `has_item(item_id, amount) -> bool`, `get_item_count(item_id) -> int`, `clear_inventory()`, `get_all_items() -> Array`.
  - [x] Сигнали: `inventory_updated`, `slot_changed(slot_index)`.
  - [x] Додати `InventoryComponent` до гравця `Player.tscn`.
* **Промпт для генерації:**
  > "Реалізуй універсальний типізований компонент `InventoryComponent.gd` з обмеженою кількістю слотів, підтримкою стакування предметів за `item.max_stack`, методами додавання/видалення та сигналом `inventory_updated`."
* **Критерій готовності:** Юніт-тест: додавання 70 одиниць дерева зі стаком 64 коректно займає 2 слоти (64 і 6).

---

### Ітерація 4.3: Природні об'єкти на карті (Дерево, Камінь) та дроп
* **Мета:** Створити об'єкти, які блокують клітинку сітки та добуваються гравцем при взаємодії.
* **Чекліст:**
  - [x] Створити `src/world/WorldResourceNode.gd` (`StaticBody2D`).
  - [x] Реєстрація позиції як твердої у `GridManager.set_cell_solid(cell, true)`.
  - [x] Метод `harvest(damage, tool_type)`: віднімає міцність, при 0 спавнить дроп і звільняє клітинку в `GridManager`.
  - [x] Створити сутність `src/entities/items/DroppedItem.tscn` (підбирається гравцем при наближенні та магнітиться).
  - [x] Додати взаємодію для гравця: удар по ресурсу на 'E' або лівим кліком миші.
  - [x] Процедурний спавн дерев, каменів та ягід на карті світу у `World.gd`.
* **Промпт для генерації:**
  > "Створи `WorldResourceNode.gd` для дерев і каміння: об'єкт блокує свою клітинку в `GridManager`, має здоров'я, реагує на удари інструментом, при знищенні звільняє клітинку сітки та спавнить `DroppedItem.gd`, який магнітиться до гравця і потрапляє в інвентар."
* **Критерій готовності:** Гравець підходить до дерева, рубає його (кнопка 'E' або клік), дерево зникає, з'являється предмет у вигляді колоди, гравець підбирає його в інвентар, клітинка знову стає прохідною.

---

### Ітерація 4.4: UI швидкого доступу (Hotbar) та Інвентар гравця
* **Мета:** Дати гравцю змогу бачити свій інвентар на екрані з адаптивним масштабуванням під 1920x1080 та 2560x1440.
* **Чекліст:**
  - [X] Створити HUD з панеллю гарячих клавіш (Hotbar на 8 слотів) з використанням відносних якорів (Anchor Presets).
  - [X] Створити вікно повного інвентаря на клавішу `Tab` / `I`.
  - [X] Підключити UI до сигналу `inventory_updated` гравця.
  - [X] Перевірити коректність розмірів слотів та тексту на 1920x1080 та 2560x1440.
* **Промпт для генерації:**
  > "Створи `src/ui/hud/HotbarUI.gd` та `InventoryUI.gd` на базі Control-вузлів: адаптивна верстка (Anchor Presets) під роздільні здатності 1920x1080 та 2560x1440, відображення іконок предметів, лічильника кількості в стаку, вибір активного слота цифрами 1-8 або колесом миші."
* **Критерій готовності:** Зібрані колоди та каміння відображаються у слотах на екрані з кількістю, інтерфейс не розмивається і не зміщується при роздільних здатностях 1920x1080 та 2560x1440.

---

# Етап 5: Крафт та Перші інструменти (Crafting System)

### Ітерація 5.1: Схема рецептів та менеджер крафту
* **Мета:** Дозволити крафтити базові інструменти згідно з `skill-resource-schema.md`.
* **Чекліст:**
  - [ ] Створити `src/data/schemas/RecipeData.gd`.
  - [ ] Створити базові рецепти: `craft_stone_axe.tres` (2 дерева + 2 кременя), `craft_stone_pickaxe.tres`, `craft_campfire.tres`.
  - [ ] Створити `src/systems/crafting/CraftingManager.gd`: перевірка наявності інгредієнтів в інвентарі, списання матеріалів, видача результату.
* **Промпт для генерації:**
  > "Створи систему крафту `CraftingManager.gd` та схему `RecipeData.gd`. Додай перевірку доступності рецептів для поточної епохи та списання ресурсів з інвентаря."
* **Критерій готовності:** Гравець з 2 деревами і 2 кременями може натиснути «Скрафтити сокиру» — ресурси списуються, в інвентар додається `stone_axe`.

---

# Етап 6: Розміщення споруд та Креслення (Placement & Blueprint Preview)

### Ітерація 6.1: Схема будівлі та система Blueprint Preview
* **Мета:** Дозволити гравцеві обирати споруду і бачити напівпрозорий привид (ghost) перед будівництвом.
* **Чекліст:**
  - [ ] Створити `src/data/schemas/BuildingData.gd` (`size_in_tiles`, `is_solid`, `construction_cost`, `build_time`).
  - [ ] Створити `src/systems/building/BuildingPlacementController.gd`.
  - [ ] Реалізувати слідування привида споруди за мишею з прив'язкою до сітки (`GridManager.world_to_map`).
  - [ ] Зелене/червоне підсвічування привида (якщо клітинки зайняті або не прохідні — червоний, якщо вільні — зелений).
* **Промпт для генерації:**
  > "Реалізуй контролер розміщення споруд `BuildingPlacementController.gd`: привид прив'язується до сітки 32x32, перевіряє через `GridManager.is_cell_walkable()` всі клітинки площі будівлі та фарбує прев'ю у зелений або червоний колір."
* **Критерій готовності:** При виборі будівлі курсор показує сітку споруди, червоніє над деревом/водою і зеленіє на чистій галявині.

---

### Ітерація 6.2: Будівельний майданчик (Construction Site Node)
* **Мета:** Створити фізичний об'єкт незавершеної споруди на карті після кліку.
* **Чекліст:**
  - [ ] Створити `src/world/ConstructionSite.gd`.
  - [ ] Майданчик блокує клітинки під собою в `GridManager`.
  - [ ] Майданчик має список необхідних ресурсів (`required_materials`) і свій внутрішній буфер для них.
  - [ ] Візуальний прогрес-бар необхідних матеріалів та часу будівництва.
* **Промпт для генерації:**
  > "Створи `ConstructionSite.gd`: об'єкт, який спавниться при розміщенні будівлі, блокує свої тайли в `GridManager`, приймає ресурси, має прогрес завершення від 0% до 100% і після готовності замінюється на фінальну будівлю."
* **Критерій готовності:** Клік мишею ставить будівельний майданчик, клітинка стає твердою для пошуку шляхів, на майданчику пишеться: «Потрібно: 10 дерева».

---

# Етап 7: Склади та Логістика предметів (Stockpiles & Containers)

### Ітерація 7.1: Скриня та зона складу (Stockpile)
* **Мета:** Створити місце, куди складаються ресурси колонії.
* **Чекліст:**
  - [ ] Створити `src/world/buildings/StockpileBuilding.gd` з великим `InventoryComponent`.
  - [ ] Реєстрація складу в глобальному списку сховищ колонії (`ColonyLogistics.gd`).
  - [ ] Можливість для гравця відкрити UI складу та перекласти предмети туди/назад.
* **Промпт для генерації:**
  > "Створи сутність складу/скрині `StockpileBuilding.gd`, що містить `InventoryComponent`, реєструється в глобальній системі логістики поселення `LogisticsManager.gd` та надає методи `get_available_item_count(item_id)` і `withdraw_item(item_id, count)`."
* **Критерій готовності:** Гравець може покласти колоди у скриню, логістичний менеджер підтверджує наявність 10 колод у мережі колонії.

---

# Етап 8: Агент-Колоніст та FSM (Colonist Agent & Navigation)

### Ітерація 8.1: Автомат станів і базова сцена колоніста
* **Мета:** Створити сутність жителя згідно з `skill-fsm-generator.md` та навігацією за сіткою.
* **Чекліст:**
  - [ ] Створити `State.gd` та `StateMachine.gd` у `src/entities/fsm/`.
  - [ ] Створити сцену `src/entities/colonist/Colonist.tscn` (`CharacterBody2D`).
  - [ ] Додати стани: `ColonistIdleState.gd` та `ColonistMoveToState.gd`.
  - [ ] Рух за точками шляху з `GridManager.get_world_path()`.
* **Промпт для генерації:**
  > "Створи базову сутність жителя `Colonist.gd` та автомат станів на базі [skill-fsm-generator.md](file:///C:/Users/Sasha/OneDrive%20-%20UCU/Робочий%20стіл/work_dir/personal/saecula_2/.agents/skills/skill-fsm-generator.md). Реалізуй рух колоніста точками шляху від `GridManager` без застрягання в кутах."
* **Критерій готовності:** Можна вказати колоністу цільову точку кліком миші, і він коректно обійде дерева по сітці та зупиниться в пункті призначення.

---

# Етап 9: Система завдань Minecolonies (Job Manager & Work Orders)

### Ітерація 9.1: Менеджер завдань колонії (`JobManager.gd`)
* **Мета:** Реалізувати систему доручень згідно з `skill-job-system.md`.
* **Чекліст:**
  - [ ] Створити `Job.gd` з полями типу завдання (`BUILD`, `HARVEST`, `HAUL`), статусу та пріоритету.
  - [ ] Створити Autoload `src/systems/jobs/JobManager.gd`.
  - [ ] Методи: `create_job()`, `request_job()`, `complete_job()`, `cancel_or_release_job()`.
* **Промпт для генерації:**
  > "Реалізуй глобальну чергу замовлень `JobManager.gd` за шаблоном з [skill-job-system.md](file:///C:/Users/Sasha/OneDrive%20-%20UCU/Робочий%20стіл/work_dir/personal/saecula_2/.agents/skills/skill-job-system.md). Додай підтримку пріоритетів та повернення завдання в чергу у разі невдачі."
* **Критерій готовності:** Вільний колоніст у стані `Idle` запитує завдання через `JobManager.request_job()` та переходить у виконання.

---

### Ітерація 9.2: Завдання вирубки лісу (Harvest Job)
* **Мета:** Гравець позначає дерево для зрубу $\to$ колоніст іде і рубає його.
* **Чекліст:**
  - [ ] Інструмент виділення дерев гравцем («Позначити на вирубку»).
  - [ ] Створення завдання `Job.JobType.HARVEST` на клітинці дерева.
  - [ ] Стан колоніста `ColonistHarvestState.gd`: підійти до сусіднього тайла, програвати анімацію удару, завдавати шкоди дереву, завершити завдання.
* **Промпт для генерації:**
  > "Створи інструмент виділення об'єктів для вирубки/збору та стан `ColonistHarvestState.gd`: колоніст отримує замовлення, прокладає шлях до дерева, рубає його і сповіщає `JobManager.complete_job()`."
* **Критерій готовності:** Гравець виділяє рамкою 3 дерева, житель почергово підходить до кожного, зрубує їх, колоди падають на землю.

---

### Ітерація 9.3: Завдання перенесення ресурсів (Haul Job)
* **Мета:** Колоністи збирають ресурси з землі та відносять на склад.
* **Чекліст:**
  - [ ] Створення завдання `Job.JobType.HAUL` для кожного `DroppedItem` на карті.
  - [ ] Стан колоніста `ColonistHaulState.gd`: підійти до предмета $\to$ підібрати в інвентар $\to$ знайти найближчий склад $\to$ піти до складу $\to$ перекласти предмети.
* **Промпт для генерації:**
  > "Реалізуй завдання та стан перенесення ресурсів `ColonistHaulState.gd`: житель забирає безгосподарні предмети з землі та відносить їх до найближчого зареєстрованого сховища `StockpileBuilding`."
* **Критерій готовності:** Після зрубу дерев житель самостійно підбирає колоди і складає їх у скриню складу.

---

# Етап 10: Будівельник і покрокове будівництво (Builder Colonist)

### Ітерація 10.1: Повний цикл будівництва Minecolonies-типу
* **Мета:** Колоніст приносить матеріали зі складу на будмайданчик і зводить споруду.
* **Чекліст:**
  - [ ] Стан `ColonistBuildState.gd`.
  - [ ] Логіка кроків будівельника:
    1. Перевірити нестачу матеріалів на майданчику (`ConstructionSite`).
    2. Якщо матеріали є на складі — піти на склад, забрати, принести на майданчик.
    3. Якщо всі матеріали на майданчику — стояти поруч і витрачати `build_time`.
    4. Завершити будівництво (будмайданчик перетворюється на готовий будинок).
* **Промпт для генерації:**
  > "Створи стан `ColonistBuildState.gd`: реалізуй повний цикл будівельника Minecolonies — доставка відсутніх матеріалів зі складу на будмайданчик, наступний процес будівництва з таймером та перетворення майданчика на фінальну будівлю."
* **Критерій готовності:** Гравець ставить креслення хатини $\to$ будівельник бере дерево зі складу $\to$ несе на майданчик $\to$ стукає молотком $\to$ з'являється повноцінна хатина.

---

# Етап 11: Професії та Будинки робітників (Worker Huts & Roles)

### Ітерація 11.1: Хатини професій (Builder Hut, Lumberjack Hut)
* **Мета:** Прив'язка робітників до конкретних споруд для спеціалізації.
* **Чекліст:**
  - [ ] Створити `WorkerHut.gd`: будинок наймає 1 жителя і призначає йому професію (`builder`, `lumberjack`).
  - [ ] Фільтрація завдань у `JobManager`: лісоруб бере лише `HARVEST`, будівельник — `BUILD`.
  - [ ] Рівні хатини: з покращенням будівлі робітник працює швидше.
* **Промпт для генерації:**
  > "Створи базову систему хатин професій `WorkerHut.gd`: призначення колоніста на посаду, фільтрація доступних завдань за професією в `JobManager.request_job()` та радіус робочої зони."
* **Критерій готовності:** Житель, призначений у Хатину Будівельника, ігнорує вирубку лісу і займається виключно будівництвом.

---

# Етап 12: Система Епох та Дослідження (Era Progression: Stone -> Bronze)

### Ітерація 12.1: Менеджер епох та Стіл досліджень
* **Мета:** Реалізувати технологічний прогрес від Кам'яного віку до Бронзового.
* **Чекліст:**
  - [ ] Створити `src/core/EraManager.gd` з поточною епохою (`stone_age`).
  - [ ] Створити ресурс `bronze_age.tres` з умовами переходу (наприклад: 30 дерева, 20 каменю, збудована Ратуша).
  - [ ] Стіл досліджень / Ратуша: будівля, де накопичуються ресурси для переходу в нову епоху.
  - [ ] Сигнал `EventBus.era_advanced(new_era_id)`: розблоковує нові рецепти та будівлі.
* **Промпт для генерації:**
  > "Створи `EraManager.gd` та логіку технологічного переходу між епохами. Реалізуй блокування рецептів та будівель, якщо їхня `required_era_id` вища за поточну, та подію переходу `era_advanced`."
* **Критерій готовності:** Гравець здає ресурси у стіл досліджень $\to$ гра переходить у Бронзовий вік $\to$ у меню будівництва відкриваються мідна піч та дощаті будинки.

---

# Етап 13: Збереження та Завантаження (Save/Load Serialization)

### Ітерація 13.1: Повна серіалізація світу у JSON
* **Мета:** Збереження стану світу, сітки, інвентарів та колоністів згідно з `skill-save-system.md`.
* **Чекліст:**
  - [ ] Створити `src/core/SaveManager.gd`.
  - [ ] Контракти `to_dict()` та `from_dict()` для: `Player`, `GridManager`, `StockpileBuilding`, `Colonist`, `EraManager`.
  - [ ] Збереження у `user://saves/slot_1.json` та завантаження світу.
* **Промпт для генерації:**
  > "Реалізуй `SaveManager.gd` спираючись на [skill-save-system.md](file:///C:/Users/Sasha/OneDrive%20-%20UCU/Робочий%20стіл/work_dir/personal/saecula_2/.agents/skills/skill-save-system.md): збереження в JSON стану епохи, позиції та інвентаря гравця, споруд на сітці та списку колоністів з їхніми станами."
* **Критерій готовності:** Гравець будує склад, наймає колоніста, зберігає гру (F5), перезапускає проект, натискає (F9) — усі будівлі, предмети і жителі відновлюються на своїх місцях.

---

# Етап 14: UI, Полірування та Зворотний зв'язок (HUD & Polish)

### Ітерація 14.1: HUD поселення та спливаючий текст (Floating Text)
* **Мета:** Зробити гру інформативною, живою та приємною з повною підтримкою 1920x1080 та 2560x1440.
* **Чекліст:**
  - [ ] Спливаючий текст над об'єктами (`+1 Дерево`, `Немає ресурсів!`, `Будівництво завершено!`).
  - [ ] Верхня панель HUD: назва поточної епохи, кількість жителів, вільні робітники з адаптивним позиціонуванням на 1080p і 1440p.
  - [ ] Звукові хуки (події для звуку кроків, удару сокири, закінчення будівництва).
* **Промпт для генерації:**
  > "Створи верхню панель HUD з лічильником населення і поточною епохою, адаптовану під роздільні здатності 1920x1080 та 2560x1440, а також систему спливаючого тексту `FloatingText.gd` для відображення зібраних ресурсів та статусів робітників."
* **Критерій готовності:** При зборі предметів красиво випливають написи над головою, у верхній панелі видно статус поселення, верстка чітка і не "пливе" на моніторах 1080p та 1440p.
